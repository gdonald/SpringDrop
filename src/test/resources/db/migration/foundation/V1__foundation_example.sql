-- Owned by the foundation test module. Content's migration references it, so it
-- only applies cleanly when modules migrate in dependency order.
create table foundation_example (
    id integer primary key
);
