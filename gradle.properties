org.gradle.parallel=true
# The build cache is intentionally off: caching test/JaCoCo task outputs can
# restore stale coverage execution data and make the 100% gate read incorrectly.
org.gradle.caching=false
