package dev.springdrop.modules.example;

/**
 * A trivial contract the example module contributes a plugin for, demonstrating
 * that a module subproject can provide plugins against the kernel.
 */
public interface ExampleService {

    String describe();
}
